#include <stdio.h>

int main()
{
	float *Notes;
	int p;
	printf("Entrer la taille du tableau : \n");
	scanf("%d",&p)
	Notes=(float*)malloc(sizeof(float));
	for(i=0;i<p;i++)
	{
		printf("Entrer la Notes N%d",i)
		scanf("%f",&Note[i])
	}
}